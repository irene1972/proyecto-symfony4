{% extends 'base.html.twig' %}

{% block title %}Registro de usuarios{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Registro de usuarios</h1>
        {# iniciamos formulario #}
        {{form_start(form)}}
        {# imprimimos los campos del formulario #}
        {{form_widget(form)}}
        {# cerramos formulario #}
        {{form_end(form)}}

    </div>
{% endblock %}
